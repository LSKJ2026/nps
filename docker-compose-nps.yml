services:
  nps:
    build:
      context: .
      dockerfile: Dockerfile.nps
      args:
        GOPROXY: ${GOPROXY:-https://goproxy.cn,direct}
    image: nps-local:latest
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./conf:/conf
